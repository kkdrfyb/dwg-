<Window x:Class="DwgOfficeToolbox.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="设置" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F7FA">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4338CA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#3730A3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="20" Height="20">
                                <Border x:Name="Border" 
                                        CornerRadius="4" 
                                        BorderBrush="#E5E7EB" 
                                        BorderThickness="2" 
                                        Background="White"/>
                                <Path x:Name="CheckMark" 
                                      Width="12" 
                                      Height="12" 
                                      Stretch="Fill" 
                                      Fill="#4F46E5" 
                                      Data="M 3 8 L 7 12 L 15 3" 
                                      Stroke="#4F46E5" 
                                      StrokeThickness="2" 
                                      StrokeLineJoin="Round" 
                                      Visibility="Collapsed"/>
                            </Grid>
                            <ContentPresenter Margin="12,0,0,0" 
                                              VerticalAlignment="Center" 
                                              TextBlock.FontSize="14" 
                                              TextBlock.Foreground="#374151"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4F46E5"/>
                                <Setter TargetName="Border" Property="Background" Value="#EEF2FF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4F46E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="#E5E7EB" 
                                BorderThickness="1" 
                                CornerRadius="12"
                                Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter Grid.Row="1"/>
                                <TextBlock Grid.Row="0" 
                                           Text="{TemplateBinding Header}" 
                                           FontWeight="SemiBold" 
                                           FontSize="16" 
                                           Foreground="#1F2937"
                                           Margin="0,0,0,16"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
            <StackPanel>
                <GroupBox Header="ODA 转换器">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ODA 路径：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                        <TextBox x:Name="OdaPathTextBox" Grid.Column="1" Text="{Binding OdaExePath, Mode=TwoWay}"/>
                        <Button Grid.Column="2" Content="浏览" Click="BrowseOdaPath_Click"/>
                    </Grid>
                </GroupBox>
                
                <GroupBox Header="输出设置">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="输出文件夹：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <TextBox x:Name="OutputFolderTextBox" Grid.Column="1" Text="{Binding OutputFolderName, Mode=TwoWay}"/>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="输出版本：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <ComboBox x:Name="OutputVersionComboBox" Grid.Column="1" SelectedValue="{Binding OutputVersion, Mode=TwoWay}">
                                <ComboBoxItem>ACAD2000</ComboBoxItem>
                                <ComboBoxItem>ACAD2004</ComboBoxItem>
                                <ComboBoxItem>ACAD2007</ComboBoxItem>
                                <ComboBoxItem>ACAD2010</ComboBoxItem>
                                <ComboBoxItem>ACAD2013</ComboBoxItem>
                                <ComboBoxItem>ACAD2018</ComboBoxItem>
                                <ComboBoxItem>ACAD2021</ComboBoxItem>
                            </ComboBox>
                        </Grid>
                        <Grid Grid.Row="2" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="输出格式：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <ComboBox x:Name="OutputFormatComboBox" Grid.Column="1" SelectedValue="{Binding OutputFormat, Mode=TwoWay}">
                                <ComboBoxItem>DXF</ComboBoxItem>
                                <ComboBoxItem>DWG</ComboBoxItem>
                            </ComboBox>
                        </Grid>
                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="输入过滤：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <TextBox x:Name="InputFilterTextBox" Grid.Column="1" Text="{Binding InputFilter, Mode=TwoWay}"/>
                        </Grid>
                    </Grid>
                </GroupBox>
                
                <GroupBox Header="性能设置">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="转换并发数：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <TextBox x:Name="MaxConvertConcurrencyTextBox" Grid.Column="1" Text="{Binding MaxConvertConcurrency, Mode=TwoWay}" TextAlignment="Right"/>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="解析并发数：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <TextBox x:Name="MaxParseConcurrencyTextBox" Grid.Column="1" Text="{Binding MaxParseConcurrency, Mode=TwoWay}" TextAlignment="Right"/>
                        </Grid>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="大文件阈值(MB)：" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14" Foreground="#6B7280"/>
                            <TextBox x:Name="LargeFileThresholdTextBox" Grid.Column="1" Text="{Binding LargeFileThresholdMB, Mode=TwoWay}" TextAlignment="Right"/>
                        </Grid>
                    </Grid>
                </GroupBox>
                
                <GroupBox Header="高级设置">
                    <StackPanel Orientation="Vertical">
                        <CheckBox x:Name="EnableTextCacheCheckBox" Content="启用文字缓存" IsChecked="{Binding EnableTextCache, Mode=TwoWay}" Margin="0,0,0,12"/>
                        <CheckBox x:Name="KeepConvertedDxfCheckBox" Content="保留转换的DXF文件" IsChecked="{Binding KeepConvertedDxf, Mode=TwoWay}" Margin="0,0,0,12"/>
                        <CheckBox x:Name="ClearOutputBeforeConvertCheckBox" Content="转换前清空输出目录" IsChecked="{Binding ClearOutputBeforeConvert, Mode=TwoWay}"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="取消" Click="CancelButton_Click" Background="#6B7280"/>
            <Button x:Name="SaveButton" Content="保存" Click="SaveButton_Click" Background="#10B981"/>
        </StackPanel>
    </Grid>
</Window>
